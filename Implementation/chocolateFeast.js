function chocolateFeast(n, c, m) {
    //n = amount of starting money 
    //c = cost of a chocolate bar 
    //m = number of wrappers necessary to get another chocolate bar 
    if (c > n) return 0
    let wrappers = 0;
    let count = 0;

}

console.log()